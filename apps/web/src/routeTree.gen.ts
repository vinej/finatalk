// AUTO-GENERATED — replaced by @tanstack/router-plugin on `vite dev`.
// This stub exists so the project type-checks before the first dev run.
import { createRootRoute } from "@tanstack/react-router";

export const Route = createRootRoute({});
export const routeTree = Route;
